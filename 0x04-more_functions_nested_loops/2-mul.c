#include "main.h"
/**
 * mul -  checks for checks for a digit (0 through 9).
 * @m: m -  Variable
 * @n: n - variable
 * Return: Always 0.
 */
int mul(int m, int n)
{
	int mul;

	mul = m * n;

	return (mul);
}
