#include "main.h"
/**
* _isupper - checks for uppercase character
* @a: Variable text
* Return: Always 0.
*/
int _isupper(int a)
{


	if (a >= 'A' && a <= 'Z')
	{
	return (1);
	}
	else
{
	return (0);
}

}
