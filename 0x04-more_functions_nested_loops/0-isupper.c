#include "main.h"
/**
* _isupper - checks for uppercase character
* @num: Variable text
* Return: Always 0.
*/
int _isupper(int num)
{


	if (num >= 'A' && num <= 'Z')
	{
	return (1);
	}
	else
{
	return (0);
}

}
