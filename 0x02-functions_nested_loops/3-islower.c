#include "main.h"

/**
 * _islower - checks for lowercase character
 * @m: the character to check
 * Return: 1 if m is lowercase, 0 otherwise
 */
int _islower(int m)
{
	return (m >= 'a' && m <= 'z');
}
