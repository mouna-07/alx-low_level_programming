#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @m: the int to check
 * Return: the absolute value of m
 */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	return (-m);
}
